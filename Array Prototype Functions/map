Array.prototype.mymap = function(callback) {
	let array =[];
  for(i=0;i<this.length;i++){
  	array.push(callback(this[i],i,this));
  }
  return array;
};

var arr1=[1,2,3,4];

var callbackFunction =(x)=>x*2
console.log(arr1.mymap(callbackFunction));